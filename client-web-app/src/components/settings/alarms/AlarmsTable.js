import React, { useCallback, useRef } from 'react';
import { AgGridReact } from 'ag-grid-react';

const AlarmsTable = () => {
    const gridRef = useRef();

    return <div>AlarmsTable</div>;
};

export default AlarmsTable;
